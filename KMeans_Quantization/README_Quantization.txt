Code is running under Jupyter Notebook.
Keep image data and code in the same derectory.
The KMeans method form sklearn.cluster libary is used.
User could specify input rasters and n_color when calling function "quantize(raster, n_colors)"