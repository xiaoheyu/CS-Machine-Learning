The code is running under Jupyter Notebook
The code should be in the same folder with InitialSeeds.txt and Tweets.json files.
Call the tweets_k_means() function with default parameters to export results. 